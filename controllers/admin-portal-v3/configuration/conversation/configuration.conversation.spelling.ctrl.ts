import { createRouter } from "../configuration.page.ctrl";

import {ConfigurationConversationSpelling} from "../../../../models/admin-portal-v3/configuration/conversation/configuration.conversation.spelling.model";

export const router = createRouter(null, null, ConfigurationConversationSpelling);
