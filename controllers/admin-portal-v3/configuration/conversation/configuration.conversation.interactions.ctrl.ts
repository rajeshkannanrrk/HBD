import { createRouter } from "../configuration.page.ctrl";

import { ConfigurationConversationInteractions } from "../../../../models/admin-portal-v3/configuration/conversation/configuration.conversation.interactions.model";

export const router = createRouter(null, null, ConfigurationConversationInteractions);
