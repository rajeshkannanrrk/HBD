import { createRouter } from "../configuration.page.ctrl";

import { ConfigurationConversationHandoff } from "../../../../models/admin-portal-v3/configuration/conversation/configuration.conversation.handoff.model";

export const router = createRouter(null, null, ConfigurationConversationHandoff);
