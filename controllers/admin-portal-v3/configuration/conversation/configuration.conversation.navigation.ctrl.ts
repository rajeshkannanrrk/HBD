import { createRouter } from "../configuration.page.ctrl";

import { ConfigurationConversationNavigation } from "../../../../models/admin-portal-v3/configuration/conversation/configuration.conversation.navigation.model";

export const router = createRouter(null, null, ConfigurationConversationNavigation);
