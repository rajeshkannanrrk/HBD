import { createRouter } from "../configuration.page.ctrl";

import { ConfigurationComplianceSecurity } from "../../../../models/admin-portal-v3/configuration/compliance/configuration.compliance.security.model";

export const router = createRouter(null, null, ConfigurationComplianceSecurity);
