import { createRouter } from "../configuration.page.ctrl";

import { ConfigurationComplianceTermsAndConsent } from "../../../../models/admin-portal-v3/configuration/compliance/configuration.compliance.terms-and-consent.model";

export const router = createRouter(null, null, ConfigurationComplianceTermsAndConsent);
