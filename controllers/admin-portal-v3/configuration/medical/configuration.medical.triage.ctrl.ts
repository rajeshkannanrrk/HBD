import { createRouter } from "../configuration.page.ctrl";

import { ConfigurationMedicalTriage } from "../../../../models/admin-portal-v3/configuration/medical/configuration.medical.triage.model";

export const router = createRouter(null, null, ConfigurationMedicalTriage);
